current_task: "oauth_implementation"
current_iteration: 0
last_verification: null

feedback_history: []

# Next iteration guidance
next_iteration:
  focus_areas:
    - "Implement OAuth URL generation with PKCE"
    - "Add proper state parameter handling"
    - "Create database migration for soundcloud_users"
  
  avoid_areas:
    - "Don't implement track fetching (separate task)"
    - "Don't create UI components (handled by templates)"
  
  specific_hints: []
  
  escalation_level: 0  # 0: normal, 1: hint provided, 2: alternative approach

# Code patterns to use
recommended_patterns:
  go_patterns:
    - "Use existing PocketBase migration patterns"
    - "Follow Go 1.23 conventions from AGENTS.md"
    - "Use echo framework patterns from existing code"
    
  security_patterns:
    - "Implement PKCE with SHA256 for code challenges"
    - "Store tokens encrypted, not plain text"
    - "Use secure random state parameters"

# Context files for reference
reference_files:
  - "main.go"
  - "pb_migrations/"
  - "AGENTS.md"

# Previous sub-agent results (when available)
previous_results: []
